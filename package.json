{
  "name": "molecvis",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/desktop",
    "packages/protocol"
  ],
  "scripts": {
    "dev": "npm run build --workspace @molecvis/protocol && npm run dev --workspace @molecvis/desktop",
    "build": "npm run build --workspace @molecvis/protocol && npm run build --workspace @molecvis/desktop",
    "test": "npm run test --workspace @molecvis/protocol && npm run test --workspace @molecvis/desktop",
    "test:chemistry": "python -m pytest services/chemistry/tests"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "minimatch": "3.1.4"
  },
  "overrides": {
    "minimatch": "3.1.4"
  }
}
